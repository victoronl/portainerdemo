apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: portainer-demo-ingress
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        backend:
          serviceName: nginx-service
          servicePort: 30080
